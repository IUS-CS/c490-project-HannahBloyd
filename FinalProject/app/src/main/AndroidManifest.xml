<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.finalproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TimeFighter"></activity>
        <activity android:name=".TimeFighterEnd"></activity>
        <activity android:name=".HowToTimeFighter"></activity>
        <activity android:name=".CFViewTesting"></activity>
        <activity android:name=".CFCompat"></activity>
        <activity android:name=".TFHighScores"></activity>
        <activity android:name=".HowToConnectFour"></activity>
        <activity android:name=".ConnectFour"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>